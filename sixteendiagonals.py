def can_be_extended(perm):
	i=len(perm)-1
	for j in range(i):
		if 

def diagonal(perm,n):
	if len(perm)==16:
		print(perm)
		exit()
	for i in range(n):
		if k not in perm:
			perm.append(k)
			can_be_extended(perm)



diagonal(perm = [], n = 5)